## Packages
framer-motion | For smooth page transitions and micro-interactions
lucide-react | Icon library (already in base, but ensuring availability)
canvas-confetti | For celebration effects when completing sessions
@types/canvas-confetti | Types for confetti
date-fns | For date manipulation and formatting

## Notes
- Using localStorage for persistence as requested (simulating backend behavior on client side for this specific "lite" request, although backend routes exist).
- Theme toggle applies `.dark` class to `document.documentElement`.
- Mobile-first approach with `max-w-md mx-auto` containers for the main app interface.
- Custom font 'Outfit' and 'Nunito' for the cozy feel.
